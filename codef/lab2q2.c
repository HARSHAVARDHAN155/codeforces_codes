#include<stdio.h>
#include<string.h>
#define max 100
int main()
{   
    int n;
    scanf("%d",&n);
   char  arr[n][max];
    char temp;
    for(int i=0;i<n;i++)
    {
        scanf("%s",&arr[i]);
    }
    for(int i=0;i<n-1;i++)
    {
        for(int j=i+1;j<n;j++)
        {
            if(arr[i],arr[j]>0)
            {

            }
        }
    }

}